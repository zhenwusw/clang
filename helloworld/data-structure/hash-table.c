int index_kmp(String S, String T, int pos){
  int i = pos;

  int j = 1;
  int next[255];
  get_next(T, next);

  while (i <= S[0] && j < T[0]) {

  }
}

void get_next() {
  int i = 1, j = 0;
  next[1] = 0;

  while (i < T[0]) {
  }
}
